/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["firebasestorage.googleapis.com", "spoonacular.com"], // Add Spoonacular domain here
  },
};

export default nextConfig;

